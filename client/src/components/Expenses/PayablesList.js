import React from 'react'

function PayableList(props) {

    return (
        <>
            <h1>Expenses</h1>
            - add miscCreditor (creditor model)
            - add payable transaction
            - add cash bank transaction
        </>
    )
}

export default PayableList